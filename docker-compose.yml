services:
  whisper-api:
    build: ./whisper-api
    restart: always
    ports:
      - "8030:8030"
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8030/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
